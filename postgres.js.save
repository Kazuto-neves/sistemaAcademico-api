172.17.0.1const { Client } = require('pg');
const CLIENTE = new Client({
  host: "192.168.1.9",
  user: "postgres",
  port: 3310,
  password: "mysenha",
  database: "ecommerce"
});

CLIENTE.connect();
CLIENTE.query('Select * from Produtos',
  (err, res)=>{
    if(!err){
      console.log(res.rows);
    }else{
      console.log(err.message);
    }
    CLIENTE.end;
  });
